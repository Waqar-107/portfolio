## important!!!

- When the updates are made, use `npm run build` to have the deployment files updated.
- Copy the contents of the `build` folder and replace with the contents of the root folder in `gh-pages` branch. The branch basicaly only contains the deployment files.